{
  "_comment": "Files still carrying a theme-blind dark translucent overlay \u2014 `bg-<hue>-{700,800,900}/NN` with no `dark:` prefix, which is by construction authored for a dark page (#632). This is a debt ledger, not an ignore-list to grow: `src/__tests__/theme/dark-translucent-overlay.test.ts` fails when any file not listed here gains one, and when a listed file exceeds its allowance for any single overlay. Counts are per distinct overlay, not one total per file, so swapping one hue for another does not net out. Dropping a count does NOT fail \u2014 shrink these freely. A row the file no longer contains DOES fail, so the ledger cannot be used to pre-authorise an overlay. Never raise a count to make CI pass; use the `bg-<hue>-50 dark:bg-<hue>-900/NN` pair instead.",
  "files": {}
}
