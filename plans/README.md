# Implementation Plans

Generated by the `improve` skill on 2026-06-21, against commit `e6980f3`.
Each plan is self-contained: an executor with zero prior context can run it from
the plan file + the repo. Read the whole plan before starting, honor its STOP
conditions, and update your status row when done.

These plans are also published as GitHub issues (see the **Issue** column) and
slotted into a repo-wide `PX.Y` priority scheme alongside the pre-existing
backlog — see `PRIORITIZATION.md` (or the issue labels) for the combined order.

## Execution order & status

| Plan | Title | PX.Y | Issue | Effort | Depends on | Status |
|------|-------|------|-------|--------|------------|--------|
| 001 | Single verification command + discoverable backend tests | P0.1 | #85 | S | — | TODO |
| 002 | Stop logging auth tokens & PII | P0.2 | #86 | S | — | TODO |
| 003 | Stop leaking exception detail in API responses | P0.3 | #87 | S–M | — | TODO |
| 004 | Path-traversal hardening in local cover-image delete | P0.4 | #88 | S | — | TODO |
| 005 | Correct README auth docs (better-auth, not Clerk) | P1.7 | #89 | S | — | DONE |
| 006 | Repo hygiene (stale 4.2 MB dump, unused @dnd-kit) | P3.2 | #90 | S | — | TODO |
| 007 | Split runtime vs test/load dependencies | P2.1 | #91 | M | 001 | DONE (test/load moved to `[project.optional-dependencies]`; runtime export 93→52 pkgs, no flask/locust/pytest) |
| 008 | Atomic cascade book-delete | P1.3 | #92 | M–L | 001 | DONE (retargeted to live path `delete_book`; dead `book_cascade_delete.py` removed) |
| 009 | Tests for transcription + cover-upload endpoints | P1.8 | #93 | M | 001 | DONE (transcription.py 40%→85%; cover tests target live `books.py` path; dead `book_cover_upload.py` removed) |
| 010 | books.py decomposition (map + 1 extraction) | P3.1 | #94 | L | 001 | TODO |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (one-line reason) | REJECTED (one-line rationale)

The combined `PX.Y` order across these plans **and** the pre-existing backlog is
in `PRIORITIZATION.md`. Issue titles carry their `[PX.Y]` prefix and a matching
per-issue label.

## Dependency notes

- **001 is the keystone.** 007, 008, 009, and 010 are data/dep/test/refactor
  changes that need a trustworthy test gate — do 001 first.
- **004 ↔ 009**: if 004 (path traversal) lands, 009's cover-upload tests should
  add a traversal-rejection case. If 009 lands first, 004 extends it.
- **002 ↔ 003**: both touch `books.py` logging/error lines but at different
  sites (`print()` vs `HTTPException(detail=...)`). No conflict; if both run,
  002's logger conversions stay and 003 only edits the `detail=` strings.
- **002/003 before 010**: clearing the `print()`/`str(e)` churn in books.py
  first makes the 010 extraction cleaner.

## Findings considered and rejected

- **Committed OpenAI key in `.env`** (subagent SEC-01): REJECTED as a repo
  finding — `.env` is gitignored and **not tracked** (`git ls-files` confirms),
  so there is no exposure through the repository. (Separately, rotating that
  on-disk key out-of-band is prudent if it is a real key; not a repo task.)
- **Dead cache services `ai_cache_service` / `chapter_cache_service`**
  (subagent ARCH-02/ARCH-07): REJECTED — both are imported and used
  (`ai_service.py:43`, `chapter_error_handler.py:19`).
- **Weak/default JWT secret accepted** (subagent SEC-05): REJECTED — already
  mitigated; `config.py:86-108` rejects the test/weak secret in production and
  warns when <64 chars.
- **NoSQL operator injection via book/chapter names** (subagent SEC-13):
  REJECTED — fields are pydantic-typed strings and motor parameterizes queries;
  low signal.
- **Cookie signature not verified** (subagent SEC-07): NOT PLANNED — the model
  is a random session token looked up in MongoDB (a bearer token whose value is
  the secret); signature verification is defense-in-depth only. Recorded for
  awareness, not worth a standalone plan now.
- **Over-permissive CORS** (subagent SEC-04): NOT PLANNED — `allow_origins` is a
  fixed configured list (not a wildcard); credentials + `*` methods/headers are
  gated by the origin allowlist. Low severity.
- **ObjectId push/pull mismatch in book_ids** (subagent BUG-13): REJECTED —
  create pushes `str(book.id)` and cascade pulls the same `str` id; consistent.

## Direction findings (options for the maintainer, not problems)

Not turned into execution plans here; surfaced for roadmap decisions:

- **DIR-A — Finish Clerk→better-auth cleanup.** Legacy `clerk_id` fallback
  branches remain (`dependencies.py:42,49`, `get_user_by_clerk_id` alias). Spike:
  count DB users with `clerk_id`, then migrate-or-delete the branches.
- **DIR-B — Export depth (EPUB + templates).** Overlaps existing issues #59/#60.
  `AUTO_AUTHOR_GAP_ANALYSIS.md:123-135` flags it; renderer layer exists to extend.
- **DIR-C — User-facing session/device management UI.** Session backend exists
  (`/api/v1/sessions/*`, fingerprinting, concurrent limits) with no UI to
  view/revoke devices.
